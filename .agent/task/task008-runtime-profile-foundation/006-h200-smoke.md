# Route

Task: task008-runtime-profile-foundation

Goal: verify the profile-backed action bridge on the H200 target.

Pass condition:

- H200 targeted tests pass from `/tmp` with explicit `PYTHONPATH`.
- Task007 dry-run still reports finite 994D obs, raw action, and motor target.
- At least one task006 or task007 representative command path is run after the
  migration and recorded.

Fail condition:

- H200 profile loader fails due missing dependency.
- Dry-run or rollout target values diverge unexpectedly from pre-migration
  evidence.

# Log

- 2026-05-08: H200 guarded run from `/tmp` with explicit `PYTHONPATH` failed
  targeted profile/YAML tests because local tests used
  `Path("configs/robots/unitree_g1_29dof_sonic.yaml")`. This was a test cwd
  assumption, not a runtime import failure.
- 2026-05-08: Updated the two local profile/YAML tests to derive the repository
  root from `Path(__file__).resolve().parents[1]` before loading the fixture
  YAML. Runtime loader behavior unchanged.
- 2026-05-08: Local root verification passed:
  `PYTHONPATH=src python -m pytest tests/test_robot_profile_loader.py tests/test_unitree_g1_29dof_sonic_profile_yaml.py`
  (`12 passed`; pytest cache warning only).
- 2026-05-08: Local outside-repo verification passed with absolute test paths
  and explicit `PYTHONPATH=<repo>/src` (`12 passed`; pytest cache warning only).
- 2026-05-08: Full local suite passed with `PYTHONPATH=src python -m pytest`
  (`109 passed`; pytest cache warning only).

# Review

Status: in progress.

- 2026-05-08: Read-only reviewer found no blocking issues in the cwd-robust
  profile/YAML test fix. Reviewer verified outside-repo execution with
  explicit `PYTHONPATH=<repo>/src`: `12 passed in 0.26s`.

